# 🚀 Быстрый старт - Сканер арбитража Polymarket

## 📋 Два варианта запуска

### 🟢 Вариант 1: Простой сканер (для начала)

**Не требует:**
- ❌ Настройки .env
- ❌ Redis
- ❌ Telegram
- ❌ API ключей

**Запуск:**
```bash
# Установите только requests
pip install requests

# Запустите сканер
python scripts/simple_scanner.py
```

**Что делает:**
- Сканирует 20 активных рынков Polymarket
- Ищет ситуации где `P_yes + P_no < 0.998`
- Выводит найденные возможности в консоль
- Можно выбрать: одно сканирование или непрерывный мониторинг

---

### 🔵 Вариант 2: Полный сканер (рекомендуется)

**Требует:**
- ✅ Настроенный .env файл
- ✅ Redis (опционально)
- ✅ Telegram бот (опционально)

**Запуск:**

```bash
# 1. Установите все зависимости
pip install -r requirements.txt

# 2. Скопируйте и настройте .env
cp .env.example .env
nano .env

# 3. (Опционально) Запустите Redis
docker run -d -p 6379:6379 redis:latest
# ИЛИ
sudo systemctl start redis

# 4. Запустите полный сканер
python scripts/market_explorer.py
```

**Что делает:**
- Все функции простого сканера +
- Логирование в файл `logs/bot.log`
- Цветной вывод в консоль
- Уведомления в Telegram (если настроен)
- Сохранение данных в Redis (если запущен)
- Детальная статистика

---

## 📊 Пример вывода

Когда найдена возможность, вы увидите:

```
======================================================================
🎯 АРБИТРАЖНАЯ ВОЗМОЖНОСТЬ #1
======================================================================
📊 Рынок: Will Bitcoin reach $100,000 by end of 2024?
🆔 Market ID: 0x1234...

💰 ЦЕНЫ:
   Yes: $0.4850 (объем: $250.00)
   No:  $0.5100 (объем: $180.00)
   Сумма: $0.9950

📈 ПРИБЫЛЬ:
   Процент: 0.50%
   Макс. объем: $180.00
   Ожидаемая прибыль: $0.90

⏰ Время: 14:35:22
======================================================================
```

---

## 🎯 Настройка параметров

### В `simple_scanner.py`:
```python
scanner = SimpleScanner(threshold=0.998)  # Порог для арбитража
scanner.run_continuous(interval=10)       # Интервал в секундах
```

### В `.env` (для полного сканера):
```bash
ARB_THRESHOLD=0.998           # P_yes + P_no должно быть < этого
MIN_PROFIT_PERCENT=0.2        # Минимальная прибыль в %
MIN_LIQUIDITY_USD=50          # Минимальная ликвидность
```

---

## 🔍 Как интерпретировать результаты

### ✅ Хорошая возможность:
- **Сумма цен**: < 0.995 (много спреда)
- **Прибыль**: > 0.5%
- **Объем**: > $100

### ⚠️ Осторожно:
- **Сумма цен**: 0.995-0.998 (маленький спред)
- **Прибыль**: 0.2-0.5%
- **Объем**: < $100

### ❌ Игнорировать:
- Рынки с очень низкой ликвидностью
- Приостановленные (halted) рынки
- Закрытые рынки

---

## 🛠️ Решение проблем

### Ошибка: "Failed to load markets"
```bash
# Проверьте интернет соединение
ping clob.polymarket.com

# Polymarket API может быть временно недоступен
# Подождите 1-2 минуты и попробуйте снова
```

### Ошибка: "Connection refused" (Redis)
```bash
# Redis не запущен или не установлен
# Решение: Закомментируйте строки с Redis в коде
# ИЛИ установите Redis:

# Ubuntu/Debian:
sudo apt install redis-server
sudo systemctl start redis

# macOS:
brew install redis
brew services start redis
```

### Нет возможностей найдено
Это нормально! Арбитражные возможности редки:
- Появляются 1-5 раз в час
- Существуют несколько секунд
- Требуют быстрого исполнения
- Конкуренция высока

**Совет**: Запустите сканер на 30-60 минут для накопления статистики

---

## 📈 Следующие шаги

После тестирования сканера:

1. ✅ **Engine** - Логика поиска арбитража (`engine/arb_finder.py`)
2. ✅ **Calculator** - Расчет объемов (`engine/calculator.py`)
3. ✅ **Execution** - Модуль торговли (`execution/order_manager.py`)
4. ✅ **Main Bot** - Полный бот (`main.py`)

---

## ⚠️ Важные замечания

1. **Это только сканер** - он НЕ торгует автоматически
2. **Тестируйте на малых суммах** перед реальной торговлей
3. **Комиссии съедают прибыль** - учитывайте 0.1-0.2% на каждую сделку
4. **Проскальзывание реально** - цена может измениться за время исполнения
5. **API rate limits** - не делайте слишком частые запросы

---

## 📞 Поддержка

Если нашли баг или есть вопросы:
- Создайте issue в репозитории
- Проверьте логи в `logs/bot.log`
- Убедитесь что используете последнюю версию

---

**Удачного арбитража! 🚀💰**